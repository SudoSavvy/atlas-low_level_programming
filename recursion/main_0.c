#include "main.h"
/**
 * 
 * 
*/

int main()
{
    char str[] = "hello";
    _puts_recursion(str);
    return (0);
}

int main(void)
{
    char str[] = "hello";
    _print_rev_recursion(str);
    write(1, '\n' ,1);

    return (0);
}