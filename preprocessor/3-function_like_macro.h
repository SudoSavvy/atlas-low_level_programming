#include <stdio.h>
#include "abs.h"

int main(void)
{
int a = -5;
int b = 3;
printf("ABS(%d) = %d\n", a, ABS(A));
printf("ABS(%d) = %d\n", b, ABS(b));
printf("ABS(%d) = %d\n", c, ABS(c));

return (0);
}